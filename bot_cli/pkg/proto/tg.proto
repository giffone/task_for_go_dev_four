syntax = "proto3";

package tgbot;

message Message {
    string user_name = 1;
    string text = 2;
}

message Response {
    bool status = 1;
}

service MessageService {
    rpc SendMessage (Message) returns (Response);
}