syntax = "proto3";

option go_package = "./pkg/proto/proto";

message Message {
    string user_name = 1;
    string body = 2;
}

message Response {
    bool status = 1;
    string id = 2;
}

service MessageService {
    rpc SendMessage (Message) returns (Response);
}